<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos necesarios -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.FloatingWidget"
        android:supportsRtl="true">

        <!-- LAUNCHER PRINCIPAL -->
        <activity
            android:name=".MainLauncherActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.FloatingWidget.Launcher">
            
            <!-- Intent-filter para ser Launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividad de Configuración -->
        <activity
            android:name=".WidgetConfigurationActivity"
            android:exported="false"
            android:theme="@style/Theme.FloatingWidget.Dialog" />

        <!-- Servicio Flotante -->
        <service
            android:name=".FloatingLauncherService"
            android:exported="false"
            android:enabled="true" />

        <!-- Receptor para el widget host (OPCIONAL - no necesario realmente) -->
        <!-- Lo eliminamos para evitar errores de recursos -->

    </application>
</manifest>